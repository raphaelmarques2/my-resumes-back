//import { deleteTempSchema } from './db-test';

export default async function () {
  // if (process.env.JEST_DATABASE_SCHEMA) {
  //   await deleteTempSchema(process.env.JEST_DATABASE_SCHEMA);
  // }
}
